# 断言级取证:citation_audit 扩到代码体标识符

agent 自诊"输出即结束,缺反向验证"——把这步接到现有审计剖面(同剖面:事实机械供给/判断留 agent/volatile 尾),
治"凭印象谈论自身架构"类编造(曾整段描述 completion_audit 设计然后宣布它不存在)。

- [ ] citation_audit: 反引号标识符(`含下划线` 或 `*.py`)提取 + grounded 判定(haystack 扩 system 消息/工具名/注册工具表)
- [ ] main: _inject_citation_audit 传 prefix+log(派生机制索引/AGENTS.md 是合法取证源)
- [ ] 测试: 编造标识符报/读过 grounded 不报/工具名不报/新提议措辞留判断/path:line 行为不回归

不建的(记录决策): 篇数/数字断言审计——合法算术(5+3=8 篇)必然误报,假阳性毒化提示通道;
"全文阅读"类深度断言——判断题,不机械化。
