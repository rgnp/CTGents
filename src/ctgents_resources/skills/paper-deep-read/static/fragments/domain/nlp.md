# NLP 领域 — 阅读策略

> 轴: domain=nlp | 触发: NLP 论文

1. **基准一致性**：不同 paper 是否用同一评估协议？NLP 的 prompt/format 差异可能导致不可比的 claim
2. **规模陷阱**：大模型 scaling 带来的增益不能直接归因为"方法好"
3. **可迁移性**：NLP 的方法（tokenization、attention variant、prompting strategy）是否有跨模态价值？
